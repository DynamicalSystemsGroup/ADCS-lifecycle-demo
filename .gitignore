__pycache__/
*.pyc
*.pyo
*.swp
*.swo
.venv/
*.egg-info/

# Local secrets — real credentials (e.g. FLEXO_TOKEN for the Starforge
# remote). Copy .env.example -> .env and fill it in; .env.example stays
# tracked, .env never does.
.env
.env.local
.pytest_cache/
.coverage
output/
plots/
*.DS_Store

# ROBOT validation outputs (regenerated by `make ontology-robot`)
ontology/rtm-robot-merged.ttl
ontology/rtm-robot-reasoned.ttl
ontology/rtm-robot-report.tsv

# Docker compute IPC scratch space (regenerated per run)
.docker-ipc/

# Marimo session state (regenerated on edit; not part of the notebook source)
__marimo__/
